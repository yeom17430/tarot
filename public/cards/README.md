Tarot card image assets used by the app.

- `0.png` through `77.png`: tarot card fronts
- `78.png`: shared card back

Card data in `src/data/tarotCards.ts` maps each card to `/cards/{id}.png`.
The card selection screen always uses `/cards/78.png` until selected cards are revealed.
